{
  /*import React from "react";
import Hoc from "./Hoc";

const Users = ({ data }) => {
  return (
    <>
      <h1>users</h1>
      {data.map((user) => {
        return (
          <div>
            <ol>
              <li>{user.name}</li>
            </ol>
          </div>
        );
      })}
    </>
  );
};

const SearchUsers = Hoc(<Users />, "users");

export default SearchUsers;*/
}
